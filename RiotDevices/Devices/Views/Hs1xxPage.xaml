<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Devices.Views.Hs1xxPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="OnOffToolbarItem" Text="On" Clicked="OnOffToolbarItem_Clicked" />
        <ToolbarItem x:Name="LedOffToolbarItem" Text="LedOff" Clicked="LedToolbarItem_Clicked" />
        <ToolbarItem x:Name="RebootToolbarItem" Text="Reboot" Clicked="RebootToolbarItem_Clicked" />
        <ToolbarItem x:Name="RefreshToolbarItem" Text="Refresh" Clicked="RefreshToolbarItem_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Padding="4">
            <ScrollView>
                <Grid x:Name="dataGrid" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height= "Auto"/>
                    </Grid.RowDefinitions>
                </Grid>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>